import fs from 'fs';import path from 'path';const out='dist';fs.rmSync(out,{recursive:true,force:true});fs.mkdirSync(out);for(const f of ['index.html','styles.css','app.js','manifest.json','icon.svg'])fs.copyFileSync(f,path.join(out,f));